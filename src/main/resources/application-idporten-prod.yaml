---
spring:
  datasource:
    url: jdbc:mariadb://idporten-user-service-mariadb:3306/idporten_user
    password: ${DATASOURCE_PASSWORD}
    username: user_service
  security:
    api-key: ${API_KEY}
  application:
    environment: prod
