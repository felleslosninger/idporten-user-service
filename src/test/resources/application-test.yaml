---
project:
  version: junit

server:
  port: 7070
spring:
  datasource:
    url: "jdbc:h2:mem:idportenusers,MODE=MySQL;DB_CLOSE_ON_EXIT=FALSE"
    driver-class-name: org.h2.Driver
    username: sa
    password: password
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
    hibernate:
      ddl-auto: none
    generate-ddl: false
  flyway:
    table: flyway_schema_history
