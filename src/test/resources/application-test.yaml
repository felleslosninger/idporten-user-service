---
project:
  version: junit

server:
  port: 7070
spring:
  datasource:
    url: "jdbc:h2:mem:idporten_user,MODE=MySQL"
    driver-class-name: org.h2.Driver
    username: sa
    password: password
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
    hibernate:
      ddl-auto: none
    generate-ddl: false


# App-specific config
idporten-user-service:
  features:
    allow-real-pid: false
    allow-synthetic-pid: true
